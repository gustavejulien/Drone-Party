{
   "private": true,
   "scripts": {
      "postinstall": "husky install ; npx husky add .husky/commit-msg 'npx commitlint --edit $1'"
   },
   "devDependencies": {
      "@commitlint/cli": "^12.1.4",
      "@commitlint/config-conventional": "^13.2.0",
      "husky": "^6.0.0"
   }
}
